{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choria.io/schemas/io.choria.fisk-ai.v1/ack.json",
  "title": "fisk-ai A2A v1 ack",
  "allOf": [
    {
      "$ref": "common.json#/$defs/header"
    }
  ],
  "properties": {
    "protocol": {
      "const": "io.choria.fisk-ai.v1.ack"
    },
    "accepted": {
      "type": "boolean"
    },
    "reason": {
      "type": "string",
      "description": "Why rejected, when accepted is false."
    },
    "conversation_token": {
      "$ref": "common.json#/$defs/conversation_token"
    },
    "max_tokens": {
      "type": "integer",
      "minimum": 0,
      "description": "Tokens this conversation may use in total, counted the same way the usage fields count them, so a client can show how much of the allowance is left. Omitted or zero when the agent sets no limit. Once a conversation reaches this value it is finished and the next prompt is refused."
    }
  },
  "required": [
    "accepted"
  ]
}
