{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choria.io/schemas/io.choria.fisk-ai.v1/error.json",
  "title": "fisk-ai A2A v1 terminal error",
  "allOf": [
    {
      "$ref": "common.json#/$defs/header"
    }
  ],
  "properties": {
    "protocol": {
      "const": "io.choria.fisk-ai.v1.error"
    },
    "stop_reason": {
      "$ref": "common.json#/$defs/stop_reason"
    },
    "error": {
      "type": "string"
    },
    "code": {
      "type": "string",
      "description": "Optional machine-readable code."
    },
    "usage": {
      "$ref": "common.json#/$defs/usage",
      "description": "What the run spent before it ended, for an ending that ran and stopped rather than one that never started. A suspended run is the case that needs it."
    },
    "trace_id": {
      "type": "string",
      "description": "The trace the run recorded, empty when the worker exports no telemetry."
    },
    "content_exported": {
      "type": "boolean",
      "description": "This turn's conversation itself reached a collector, not only the shape and timing of the run."
    }
  },
  "required": [
    "error"
  ]
}
